# Pinned toolchain for reproducible builds of the proxy evidence engine.
# Cryptographic verifiers and Wasm binaries must not drift between CI runs.
[toolchain]
channel = "1.96.1"
profile = "minimal"
components = ["clippy", "rustfmt"]
targets = ["wasm32-wasip1"]
